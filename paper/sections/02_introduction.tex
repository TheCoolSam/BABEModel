Digital platforms now structure much of the public sphere. They also concentrate disagreement: algorithmic feeds can amplify conflict and deepen affective polarization~\citep{Bail2018, Levy2021}.

For platforms, polarization is an economic problem as well as a social one. Hostile feeds raise cognitive load; users may leave. \citet{Hirschman1970} called this \textit{exit}. Exit shrinks the active audience and advertising impressions. We report step-wise advertising revenue as a \emph{readability transform of retention} under a stylized constant unsafe multiplier: in the main design the brand-safety cliff never switches, so dollar contrasts track active users rather than a changing safety regime~\citep{Guess2023, Levy2021}.

Platforms therefore experiment with structural feed interventions that reduce exposure to confrontational content before conflict escalates. We model one such intervention as a probabilistic \textbf{Suppression Filter} (short: the filter). It intercepts interactions in the latitude of rejection~\citep{Sherif1961}---messages so far from the receiver's position that exposure tends to trigger modelled backlash rather than assimilation. This is \emph{not} constructive bridging ranking that promotes cross-camp contact; it suppresses high-friction exposure. Field experiments~\citep{Guess2023, Bail2018} show that exposure regimes shape attitudes and behavior; our filter is an abstraction of suppression-oriented curation, not a claim about any specific product.

Suppression can create a trade-off we call the \textit{Polarization Paradox}: the filter sharply improves retention (and therefore step-wise revenue under our stylized scale) while \emph{active-agent} polarization stays high or rises because extreme agents who would otherwise exit remain in the communicating population. Full-cohort polarization $\sigma_{\mathrm{all}}$ (churned agents retain last opinions) does not show that rise, so the paradox is a retention-versus-survivor-dispersion trade-off rather than a warming of the entire population. In our main $2\times2$, initial polarization already sits above the stylized brand-safety cliff ($\sigma(0)\approx0.7>0.6$), so the revenue contrast is demographic.

We formalize this with Dynamic-BABE (Biased Assimilation \& Behavioral Entrenchment). Closest formal ancestors include Chen-style biased assimilation~\citep{Chen2021}, bounded-confidence opinion dynamics~\citep{Hegselmann2002, Deffuant2000}, Social Judgment repulsion~\citep{Sherif1961, Jager2005}, and algorithmic-bias / selection models that amplify fragmentation without an exit channel~\citep{Sirbu2019}. Relative to those baselines, to adaptive-network rewiring~\citep{Macy2002, Keijzer2018}, and to feed-ranking field experiments~\citep{Guess2023, Levy2021}, our contribution is the joint coupling of a \emph{suppression} filter to \emph{exit} so that retention and active-set dispersion can diverge---with $\sigma_{\mathrm{all}}$ as a required honesty check---plus separate co-evolving dyadic trust segregation on a \emph{fixed} topology. Following \citet{Slovic1993}, trust builds slowly under agreement and erodes quickly under conflict.

A $2\times2$ experiment crosses the Suppression Filter with dynamic trust. The filter dampens immediate conflict and exit, but can leave extreme agents active who would otherwise leave. Separately, trust co-evolution produces \textit{trust segregation}: trust concentrates within opinion camps while outgroup trust collapses. That pattern is distinct from the filter paradox.

Our contributions are threefold. First, Dynamic-BABE links cognition, dyadic trust, exit, and stylized retention economics in one ABM. Second, we document the Polarization Paradox as a filter$\times$exit survivorship effect on active-user polarization (with $\sigma_{\mathrm{all}}$ as a required robustness check). Third, we argue that retention metrics alone are incomplete if the goal includes relational cohesion.

\ifdefined\BlindSubmission
Replication materials will be provided to the editors as an anonymised archive for peer review; we intend to deposit the model on CoMSES Net for permanent archival.\endnote{CoMSES upload to be completed by the authors prior to publication.}
\else
Replication materials are available at \url{https://github.com/TheCoolSam/BABEModel}; we intend to deposit the model on CoMSES Net for permanent archival.\endnote{CoMSES upload to be completed by the authors prior to publication.}
\fi
